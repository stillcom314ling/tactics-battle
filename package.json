{
  "name": "ascii-tactics",
  "version": "0.1.0",
  "description": "Tactical ASCII combat game with parallax depth rendering",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "pixi.js": "^8.6.6",
    "rot-js": "^2.2.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.1"
  }
}
